import React from 'react';
import { Div } from 'styled-kit';

const NotFoundPage = props => (
  <Div flex={1} justifyCenter itemsCenter style={{ fontSize: 80, color: '#666', fontWeight: 700 }}>
    404
  </Div>
);

export default NotFoundPage;
